//get number from user those many time get number and find how many number divisible for five

#include<stdio.h>

int getNumber(){
int number;
scanf("%d",&number);
return number;
}
int divisibleByFive(int a){
   int divisible=0,manyTime;
   for(int i=1;i<=a;i++){
    printf("Enter The Number %d : ",i);
    manyTime = getNumber();

   if(manyTime % 5 == 0){
       divisible++;
   }
   }
   printf("Number Of Divisible Numbers : %d",divisible);
           return 0;
}

   void main(){
   printf("Enter The Intiger ");
   int a=getNumber();
   divisibleByFive(a);
   }
